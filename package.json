{
  "name": "@unfoldingword/editor",
  "description": "A React Component Library for editing PERF.",
  "version": "1.0.0",
  "homepage": "https://x.netlify.app/",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/x/x"
  },
  "dependencies": {
    "@xelah/core-type-perf-html": "^1.0.0"
  },
  "scripts": {
    "start": "yarn && styleguidist server",
    "build-dev": "../../node_modules/.bin/styleguidist build",
    "build": "babel src --out-dir build && cp ./src/components/*.css ./build/components",
    "test": "react-scripts test",
    "prepublishOnly": "rm -fr ./build && nx build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "main": "build/index.js",
  "files": [
    "build"
  ]
}
